<h1>Create a customer profile</h1>
<%= form_for @customer do|f| %>
	<% if @customer.errors.any? %>
		<h2><%= pluralize(@customer.errors.count, "error")  %> prohibited this article from being saved:</h2>
    	<% @customr.errors.full_messages.each do |msg| %>
      		<li><%= msg %></li>
    	<% end %>		
    <% end %>

  	<%= f.fields_for :user_attributes, @customer.user do |t| %>
	   	<div>
	    	<%= t.label :email %><br/>
	    	<%= t.email_field :email %>
	    </div>
	    <div>
	    	<%= t.label :password %><br/>
	    	<%= t.password_field :password %>
	    </div>
	    <div>
	    	<%= t.label :password_confirmation %><br/>
	    	<%= t.password_field :password_confirmation %>
	    </div>
	<% end %>

    <div>
    	<%= f.label :first_name %><br/>
    	<%= f.text_field :first_name %>
    </div>

    <div>
    	<%= f.label :last_name %><br/>
    	<%= f.text_field :last_name %>
    </div>

    <div>
    	<%= f.submit %>
    </div>

<% end %>